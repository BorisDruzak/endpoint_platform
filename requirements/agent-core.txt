# Locked neutral Endpoint Agent runtime dependencies (Python 3.12).
# Refresh deliberately; do not substitute unpinned ranges in package inputs.
aiohttp==3.13.3
aiosqlite==0.22.1
loguru==0.7.3
psutil==7.2.2
pydantic==2.12.5
PyYAML==6.0.2
